"""Compact JSON shapes returned by agents."""

AGENT_JSON_INSTRUCTION = (
    "Return a single JSON object only, no markdown fences, no extra text. "
    "Use only the keys specified in the schema description."
)
